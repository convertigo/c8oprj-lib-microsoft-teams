accessibility: Hidden
authenticatedContextRequired: true
comment: Lists transcripts for a Teams online meeting. (Graph permissions OnlineMeetingTranscript.Read.All)
↓inputVars [steps.InputVariablesStep-1772640075748]: 
  comment: Exposes request inputs in XML for debugging and source mapping.
↓ExecuteGetMeetingTranscripts [steps.SimpleStep-1772640075784]: 
  comment: Calls Microsoft Graph onlineMeeting transcripts endpoint.
  expression: |
    'include("js/graph_meetings.js");
    
    var graphOperation = "list_meeting_transcripts";
    var requiredPermission = "OnlineMeetingTranscript.Read.All";
    var tokenMode = "delegated_or_application";
    response = null;
    
    try {
    	var targetUser = gm_require("userId", userId);
    	var targetMeetingId = gm_require("onlineMeetingId", onlineMeetingId);
    	var pageTop = gm_parseInteger("top", top, 25);
    	if (pageTop < 1) {
    		pageTop = 1;
    	}
    	if (pageTop > 100) {
    		pageTop = 100;
    	}
    	
    	var tokenInfo = gm_resolveToken(accessToken, tenantId, clientId, clientSecret);
    	tokenMode = tokenInfo.mode;
    	var targetUserIdForRequest = gm_graphResolveOnlineMeetingsUserId(tokenInfo, targetUser);
    	var listed = gm_graphHttp(
    		"GET",
    		tokenInfo.token,
    		"/users/" + gm_urlEncode(targetUserIdForRequest) + "/onlineMeetings/" + gm_urlEncode(targetMeetingId) + "/transcripts",
    		{ "$top": String(pageTop) },
    		null,
    		null
    	);
    	
    	var payload = listed.data === null || listed.data === undefined ? {} : listed.data;
    	var values = gm_graphPayloadArray(listed);
    	response = gm_responseOk(graphOperation, tokenMode, requiredPermission, {
    		userId: targetUser,
    		resolvedUserId: targetUserIdForRequest,
    		onlineMeetingId: targetMeetingId,
    		count: values.length,
    		nextLink: gm_safeString(payload["@odata.nextLink"]),
    		items: values
    	});
    } catch (e) {
    	response = gm_responseError(graphOperation, tokenMode, requiredPermission, e);
    }'
↓response [steps.JsonToXmlStep-1772640075827]: 
  comment: Converts the JS response object into XML output.
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: response
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: response
↓userId [variables.RequestableVariable-1772640075882]: 
  comment: User id or UPN owning the online meeting.
  description: Organizer user id or principal name
  required: true
↓onlineMeetingId [variables.RequestableVariable-1772640075943]: 
  comment: Target online meeting identifier.
  description: Online meeting id
  required: true
↓top [variables.RequestableVariable-1772640076014]: 
  comment: Maximum number of transcripts returned.
  description: Page size
  value: 25.0
↓accessToken [variables.RequestableVariable-1772640076084]: 
  comment: Optional delegated bearer token. If provided, tenant/client/secret are ignored.
  description: Delegated Microsoft Graph access token
↓tenantId [variables.RequestableVariable-1772640076165]: 
  comment: Azure Entra tenant id used for app-only token acquisition.
  description: Tenant id for app-only token
  value: ${Microsoft_AzGraph.tenantId}
↓clientId [variables.RequestableVariable-1772640076257]: 
  comment: Azure Entra application client id used for app-only token acquisition.
  description: Client id for app-only token
  value: ${Microsoft_AzGraph.clientId}
↓clientSecret [variables.RequestableVariable-1772640076355]: 
  description: Client secret for app-only token
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: xa51e43ef1d108aa61557c46e9670abb3d50668c28091a8d24f75c1d2eb802780bc1e972ff7382dfe8bbbe42f37b74544
  visibility: '-1'
↓TC_GetMeetingTranscripts_Nominal [core.TestCase]: 
  ↓userId [variables.TestCaseVariable-1772641908690]: 
    value: user@contoso.com
  ↓onlineMeetingId [variables.TestCaseVariable-1772641908691]: 
    value: dummy-online-meeting-id
  ↓top [variables.TestCaseVariable-1772641908692]: 
    value: 1
  ↓accessToken [variables.TestCaseVariable-1772641908693]: 
  ↓tenantId [variables.TestCaseVariable-1772641908694]: 
    value: ${Microsoft_AzGraph.tenantId}
  ↓clientId [variables.TestCaseVariable-1772641908695]: 
    value: ${Microsoft_AzGraph.clientId}
  ↓clientSecret [variables.TestCaseVariable-1772641908696]: 
    value: ${Microsoft_AzGraph.clientSecret.secret}