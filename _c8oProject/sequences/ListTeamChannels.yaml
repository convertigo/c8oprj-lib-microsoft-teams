accessibility: Hidden
authenticatedContextRequired: true
comment: Lists channels for a Microsoft Teams team. (Graph permissions Channel.ReadBasic.All|Channel.Read.All)
↓inputVars [steps.InputVariablesStep-1772640080521]: 
  comment: Exposes request inputs in XML for debugging and source mapping.
↓ExecuteListTeamChannels [steps.SimpleStep-1772640080563]: 
  comment: Calls Microsoft Graph team channels endpoint.
  expression: |
    'include("js/graph_meetings.js");
    
    var graphOperation = "list_team_channels";
    var requiredPermission = "Channel.ReadBasic.All|Channel.Read.All";
    var tokenMode = "delegated_or_application";
    response = null;
    
    try {
    	var targetTeamId = gm_require("teamId", teamId);
    	var pageTop = gm_parseInteger("top", top, 50);
    	if (pageTop < 1) {
    		pageTop = 1;
    	}
    	if (pageTop > 200) {
    		pageTop = 200;
    	}
    	
    	var tokenInfo = gm_resolveToken(accessToken, tenantId, clientId, clientSecret);
    	tokenMode = tokenInfo.mode;
    	var listed = gm_graphHttp(
    		"GET",
    		tokenInfo.token,
    		"/teams/" + gm_urlEncode(targetTeamId) + "/channels",
    		{ "$top": String(pageTop) },
    		null,
    		null
    	);
    	
    	var payload = listed.data === null || listed.data === undefined ? {} : listed.data;
    	var values = gm_graphPayloadArray(listed);
    	response = gm_responseOk(graphOperation, tokenMode, requiredPermission, {
    		teamId: targetTeamId,
    		count: values.length,
    		nextLink: gm_safeString(payload["@odata.nextLink"]),
    		items: values
    	});
    } catch (e) {
    	response = gm_responseError(graphOperation, tokenMode, requiredPermission, e);
    }'
↓response [steps.JsonToXmlStep-1772640080614]: 
  comment: Converts the JS response object into XML output.
  jsonObject: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: JS
        - →→: response
  key: 
    - xmlizable: 
      - ↑classname: com.twinsoft.convertigo.beans.steps.SmartType
      - SmartType: 
        - ↑mode: PLAIN
        - →→: response
↓teamId [variables.RequestableVariable-1772640080674]: 
  comment: Target Teams team identifier.
  description: Teams team id
  required: true
↓top [variables.RequestableVariable-1772640080735]: 
  comment: Maximum number of channels returned.
  description: Page size
  value: 50.0
↓accessToken [variables.RequestableVariable-1772640080806]: 
  comment: Optional delegated bearer token. If provided, tenant/client/secret are ignored.
  description: Delegated Microsoft Graph access token
↓tenantId [variables.RequestableVariable-1772640080877]: 
  comment: Azure Entra tenant id used for app-only token acquisition.
  description: Tenant id for app-only token
  value: ${Lib_Microsoft_Teams.tenantId}
↓clientId [variables.RequestableVariable-1772640080955]: 
  comment: Azure Entra application client id used for app-only token acquisition.
  description: Client id for app-only token
  value: ${Lib_Microsoft_Teams.clientId}
↓clientSecret [variables.RequestableVariable-1772640081049]: 
  description: Client secret for app-only token
  value: 
    - ↑ciphered: true
    - ↑traceable: false
    - →→: x34f14b29e41330b3743373769e226a41671c5d009bba84fd240ca36453886bbc9bad0296cbcdb651194362fbdb31e671
  visibility: '-1'
↓TC_ListTeamChannels_Nominal [core.TestCase]: 
  ↓teamId [variables.TestCaseVariable-1772641908715]: 
    value: 00000000-0000-0000-0000-000000000000
  ↓top [variables.TestCaseVariable-1772641908716]: 
    value: 5
  ↓accessToken [variables.TestCaseVariable-1772641908717]: 
  ↓tenantId [variables.TestCaseVariable-1772641908718]: 
    value: ${Lib_Microsoft_Teams.tenantId}
  ↓clientId [variables.TestCaseVariable-1772641908719]: 
    value: ${Lib_Microsoft_Teams.clientId}
  ↓clientSecret [variables.TestCaseVariable-1772641908720]: 
    value: ${Lib_Microsoft_Teams.clientSecret.secret}